*** compute extra PH controls
g A_triangle = sqrt( area * (4/sqrt(3)) )
g H_triangle = sqrt(2*hhs + 1/4) - 1/2  - 1
g dist_triangle = A_triangle / H_triangle

g A_square  = sqrt(area)
g H_square  = sqrt(hhs) - 1
g dist_square = A_square / H_square